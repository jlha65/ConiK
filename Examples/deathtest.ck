program deathTest;

var int a[15];
    int b[10];
    int i, j;
    parabola p1;

foo (hyperbola h2)
{
    # p1 ~ "y = 4x^2 + 18x - 5";
}

{
    a[0] = 1;
    b[1] = 7;
    b[a[0]] = 9;
    # j = 1;
    # plot(p1);
    print(b[a[0]]);

    #Fill array with data
    # for(i = 0; i < 1500; i = i+1){
    #     a[i] = j;
    #     j = j + 1;
    # }

    # #Print the array :D
    # print("Printing array!");
    # for(i = 0; i < 1500; i = i+1){
    #     print(a[i]);
    # }
}